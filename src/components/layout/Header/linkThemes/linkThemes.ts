import { colors } from '@/theme/theme-variables';

export const themesDefaultActiveLinks = {
  dark: {
    linkColor: colors.darkGray,
    activeLinkColor: colors.black,
  },
  light: {
    linkColor: colors.white,
    activeLinkColor: colors.success,
  },
};
